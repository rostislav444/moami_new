<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE yml_catalog SYSTEM "shops.dtd">

<yml_catalog date="2022-07-20 14:58">
    <shop>
        <name>Guava shop</name>
        <company>Moami</company>
        <url>https://moami.com.ua/</url>
        <currencies>
            <currency id="UAH" rate="1"/>
            <currency id="USD" rate="36.6"/>
            <currency id="USD" rate="37.9"/>
        </currencies>
        <categories>
            {% for category in categories %}
                <category id="{{ category.rozetka_id }}">{{ category.name }}</category>
            {% endfor %}
        </categories>
        <offers>
            {% for product in products %}
                {% for variant in product.variants %}
                    {% for size in variant.sizes %}
                        <offer id="19305" available="true">
                            <price>{{ product.price }}</price>
                            <currencyId>UAH</currencyId>
                            <categoryId>{{ product.rozetka_category_rozetka_id }}</categoryId>
                            {% for image in variant.images %}
                                <picture>https://moami.com.ua{{ image.image }}</picture>
                            {% endfor %}
                            <vendor>{{ product.brand }}</vendor>
                            <article>{{ variant.code }}-{{ size.size }}</article>
                            <stock_quantity>{{ size.stock }}</stock_quantity>
                            <name>{{ product.name }} {{ variant.color }} ({{ variant.code }}-{{ size.size }})</name>
                            <name_ua>{{ product.name_uk }} {{ variant.color_uk }} ({{ variant.code }}-{{ size.size }})
                            </name_ua>
                            <description>
                                <![CDATA[{{ product.description }}]]>
                            </description>
                            <description_ua>
                                <![CDATA[{{ product.description_uk }}]]>
                            </description_ua>
                            {% for attribute in product.attributes %}
                            <param name="{{ attribute.attribute_group }}">{{ attribute.attributes }}</param>
                            {% endfor %}
                            <param name="Колір">{{ variant.color }}</param>
                            {% if compositions.count != 0 %}
                                <param name="Склад">{% for composition in product.compositions %}{{ composition.value }}%
                                        {{ composition.name }}{% if not forloop.last %}, {% endif %}{% endfor %}</param>
                            {% endif %}
                            <param name="Країна-виробник товару">{{ product.country }}</param>
                        </offer>
                    {% endfor %}
                {% endfor %}
            {% endfor %}
        </offers>
    </shop>
</yml_catalog>